# ── Core Framework ──────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── Database ────────────────────────────────────
sqlalchemy>=2.0.36
psycopg2-binary>=2.9.10
alembic>=1.14.0

# ── Broker: Zerodha Kite Connect ────────────────
kiteconnect>=5.0.1

# ── Broker: Angel One SmartAPI ──────────────────
smartapi-python>=1.4.2

# ── Authentication, JWT & Security ──────────────
pyotp>=2.9.0
bcrypt>=4.2.0
PyJWT>=2.9.0
python-multipart>=0.0.18
websockets>=13.0
requests>=2.32.0

# ── Async HTTP (Telegram notifications) ─────────
httpx>=0.28.0

# ── Browser Automation (fallback token refresh) ─
playwright>=1.49.0

# ── Rate Limiting ───────────────────────────────
slowapi>=0.1.9

# ── Metrics / Observability ─────────────────────
prometheus-fastapi-instrumentator>=7.0.0

# ── Scheduling (position reconciliation, cron) ──
apscheduler>=3.10.0

# ── Resilience ──────────────────────────────────
tenacity>=9.0.0

# ── Testing ─────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# ── Data / Indicators (Strategy Engine) ─────────
numpy>=1.26.0
pandas>=2.2.0

# ── Utilities ───────────────────────────────────
python-dotenv>=1.0.1
